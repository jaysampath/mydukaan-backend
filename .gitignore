node_modules/

# Environment (real values never committed; .env.example is the template)
.env
.env.dev
.env.prod
.env.local

# Supabase CLI local state (never commit linked-project credentials)
supabase/.branches
supabase/.temp
supabase/.env

.DS_Store
*.pem
